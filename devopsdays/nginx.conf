server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # always serve fresh files while iterating on demo copy
    add_header Cache-Control "no-store";

    location / {
        try_files $uri $uri/ =404;
    }
}
