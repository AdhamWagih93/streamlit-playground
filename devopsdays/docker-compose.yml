services:
  demo:
    build: .
    container_name: devopsdays-demo
    ports:
      - "8181:80"
    volumes:
      # live-mount the site so copy tweaks show on refresh without rebuilding
      - ./site:/usr/share/nginx/html:ro
    restart: unless-stopped
