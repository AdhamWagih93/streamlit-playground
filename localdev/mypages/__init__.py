# Local-dev `mypages` package. In the real deployment cicd_dashboard.py and
# cc_docchat.py live here; locally they live at the repo root, so cc_docchat.py
# below loads the root module under this package name. cicd_dashboard.py is run
# directly by front_local.py (repo-root file → your edits are tested verbatim).
